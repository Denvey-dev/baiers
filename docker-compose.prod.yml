# Перегруз compose под прод
services:
  nginx:
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infra/nginx/templates/prod.conf.template:/etc/nginx/templates/10-main.conf.template:ro
      - ./infra/nginx/certs:/etc/nginx/certs:ro